export const validateEmail = (value) => value.length > 0;
export const validateFirstName = (value) => value.length > 0;
export const validateLastName = (value) => value.length > 0;
export const validatePassword = (value) => value.length > 0;
export const validatePhone = (value) => value.length > 0;
